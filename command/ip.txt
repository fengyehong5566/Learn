[root@repo ~]# ip --help
Usage: ip [ OPTIONS ] OBJECT { COMMAND | help }
       ip [ -force ] -batch filename


OPTIONS   :=      { -V[ersion] | 打印版本信息
                    -s[tatistics] | 
                    -d[etails] | 输出更详细信息
                    -r[esolve] |
                    -h[uman-readable] | 打印易读帮助信息
                    -iec |
                    -f[amily] { inet | inet6 | ipx | dnet | mpls | bridge | link } |
                    -4 | 相当“-family inet”
                    -6 | 相当“-family inet6”
                    -I | 相当“-family ipx”
                    -D | 相当“-family decnet”
                    -B | 相当“-family bridge”
                    -M | 相当“-family mpls”
                    -0 | 相当“-family link”
                    -l[oops] { maximum-addr-flush-attempts } |
                    -br[ief] |
                    -o[neline] | 
                    -t[imestamp] | 
                    -ts[hort] | 
                    -b[atch] [filename] |
                    -rc[vbuf] [size] | 
                    -n[etns] name | 
                    -a[ll] | 
                    -c[olor]}

OBJECT := { link | 
            address | 设备的协议地址
            addrlabel | 协议地址选择的标签配置
            route | 路由表条目
            rule | 路由策略数据库中的规则
            neigh | 
            ntable | 管理邻居缓存的操作。
            tunnel | tunnel  over IP
            tuntap | 管理TUN/TAP设备
            maddress | 多播地址
            mroute | 组播路由缓存条目
            mrule | 组播路由策略数据库中的规则
            monitor | 监控netlink消息
            xfrm | 管理IPSEC策略
            netns | 管理网络名字空间
            l2tp | tunnel ethernet
            fou | 
            macsec | 
            tcp_metrics | 管理tcp指标
            token | 管理标记化的接口标识符。
            netconf | 
            ila |
            neighbour | 管理ARP或ndisk缓存条目
            vrf }